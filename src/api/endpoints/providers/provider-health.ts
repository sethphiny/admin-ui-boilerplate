export const providerHealthApi = {
  getProviderHealth: async (_provider: string) => ({}),
  getAllProvidersHealth: async () => ({}),
  getProviderMetrics: async (_provider: string) => ({}),
  getProviderErrors: async (_provider: string, _limit: number) => [],
}
